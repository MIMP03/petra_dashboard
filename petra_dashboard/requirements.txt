Flask>=3.0
flask-socketio>=5.3
python-socketio>=5.11
eventlet>=0.36

# Solo necesario si vas a correr en modo ROS (PETRA_MODE=ros).
# No se instala con pip: viene con tu distribución de ROS (rospy, sensor_msgs,
# geometry_msgs, std_msgs). Déjalo comentado si usarás pip para lo demás.
# rospy
